## 1. 中間公告看板重心對齊

- [x] 1.1 修改 `LotusWelcomeWidget.tsx` 垂直排版，使用 `justify-center gap-4` 讓元素緊湊靠攏，解決垮下塌陷的問題。

## 2. 名片大頭貼與標籤溢出修復

- [x] 2.1 修改 `page.tsx` 底段卡片，大頭貼改用 `img` 元件渲染，預設為 Lorelei 手繪風頭像 URL。
- [x] 2.2 收窄發言區高度為固定 `h-[56px] min-h-[56px]`，並將遊戲/角色標籤 `game • hero` 移至姓名下方。
- [x] 2.3 調整卡片底部 tags 的 Padding 與字體大小為 `text-[10.5px]`，完美防溢出。

## 3. 滑動更新動畫與驗證

- [x] 3.1 在首頁中加入滑動更新動畫過渡（ slideIn 關鍵幀），並為卡片套用動畫，展現左進右出的動態。
- [x] 3.2 執行 TypeScript 與型別檢查，確保編譯無誤並進行成果展示。
