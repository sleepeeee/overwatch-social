## 1. 修正對齊與置中 Bugs

- [x] 1.1 移除 Lotus SVG 圖標外層容器的 `pt-2` 確保圖標在其寬高容器中完美垂直水平置中
- [x] 1.2 在步進按鈕的 `animate-ping` 動態發光圈 div 加上定位與平移 class 以修復發光圈偏心 bug

## 2. 重構垂直佈局與重心

- [x] 2.1 移除文字公告區容器上的 `flex-grow flex flex-col justify-center` 避免其強制拉伸空間
- [x] 2.2 在文字公告區與按鈕組外側新增一個 flex 包裝容器，並使用負 margin 向上提拉，拉近與圖標距離並釋放底部空間
- [x] 2.3 限制文字公告內文 `p` 標籤的 `max-w-[280px]` 改善大螢幕下的文字斷行與聚攏感

## 3. 驗證與品質控制

- [x] 3.1 啟動開發伺服器，驗證 `LotusWelcomeWidget` 的版面配置與動態光圈效果
- [x] 3.2 執行 TypeScript 類型檢查與 Ruff 排版檢查確保無語法或風格問題
